# مدى 1.2 (Mada) - تطبيق الاتصال الآمن المتقدم

<div align="center">

![مدى Logo](./assets/images/icon.png)

[![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)](https://github.com/your-repo)
[![License](https://img.shields.io/badge/license-Private-red.svg)](./LICENSE)
[![React Native](https://img.shields.io/badge/React%20Native-0.81.4-61DAFB.svg)](https://reactnative.dev/)
[![Expo](https://img.shields.io/badge/Expo-53.0.23-000020.svg)](https://expo.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-3178C6.svg)](https://www.typescriptlang.org/)

**تطبيق محمول متطور يجمع بين الأمان الفائق والذكاء الاصطناعي والتجربة الاجتماعية الغنية**

[الميزات](#-الميزات) • [البدء السريع](#-البدء-السريع) • [التثبيت](#-التثبيت) • [التطوير](#️-التطوير) • [البنية](#-بنية-التطبيق) • [الأمان](#-الأمان) • [المساهمة](#-المساهمة)

</div>

---

## 📋 جدول المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [الميزات الرئيسية](#-الميزات-الرئيسية)
- [متطلبات النظام](#-متطلبات-النظام)
- [البدء السريع](#-البدء-السريع)
- [التثبيت التفصيلي](#-التثبيت-التفصيلي)
  - [Windows + PowerShell](#windows--powershell)
  - [Windows + Git Bash](#windows--git-bash)
  - [Linux/macOS](#linuxmacos)
- [التطوير](#️-التطوير)
- [البنية المعمارية](#-البنية-المعمارية)
- [الأمان والخصوصية](#-الأمان-والخصوصية)
- [الاختبارات](#-الاختبارات)
- [النشر](#-النشر)
- [استكشاف الأخطاء](#-استكشاف-الأخطاء-وإصلاحها)
- [الأسئلة الشائعة](#-الأسئلة-الشائعة)
- [المساهمة](#-المساهمة)
- [الترخيص](#-الترخيص)

---

## 🌟 نظرة عامة

**مدى 1.2** هو تطبيق محمول متقدم تم بناؤه باستخدام React Native وExpo، يوفر منصة اتصال اجتماعي آمنة مع ميزات الذكاء الاصطناعي المتطورة. التطبيق مصمم بأعلى معايير الأمان السيبراني ويدعم نظامي التشغيل iOS وAndroid بالإضافة إلى الويب.

### 🎯 الأهداف الرئيسية

- **الأمان أولاً**: تشفير E2EE، Secure Enclave، Key Rotation، والمزيد
- **الذكاء الاصطناعي**: محرك توصيات، تعلم آلي محلي، وتحليل سلوكي متقدم
- **التجربة الاجتماعية**: محادثات، مشاركة المحتوى، الألعاب، والمحفظة الرقمية
- **الخصوصية**: GDPR متوافق، DLP، وأدوات إدارة البيانات الشخصية

---

## ✨ الميزات الرئيسية

### 🔐 الأمان المتقدم

- **التشفير الشامل (E2EE)**: بروتوكول Signal للرسائل
- **Secure Enclave**: تخزين آمن للمفاتيح الحساسة
- **المصادقة البيومترية**: Face ID وبصمة الإصبع
- **كشف Root/Jailbreak**: حماية ضد الأجهزة المخترقة
- **Key Rotation الآلي**: تدوير المفاتيح التلقائي
- **Mobile RASP**: حماية تطبيق وقت التشغيل
- **DLP**: منع تسرب البيانات
- **UEBA**: تحليل سلوك المستخدم والكيانات
- **SOC Integration**: تكامل مع مركز عمليات الأمان

### 🤖 الذكاء الاصطناعي

- **محرك التوصيات الاجتماعية**: اقتراحات أصدقاء ذكية
- **الذكاء الاصطناعي المحلي (Edge AI)**: معالجة البيانات على الجهاز
- **الرد التلقائي على الرسائل**: مساعد محادثة ذكي
- **RAG المحلي**: استرجاع المعلومات المحلي
- **A/B Testing**: تحسين التجربة بناءً على البيانات
- **Federated Learning**: تعلم آلي موحد محافظ على الخصوصية

### 💬 التواصل الاجتماعي

- **محادثات آمنة**: رسائل مشفرة نهاية إلى نهاية
- **الرسائل ذاتية التدمير**: رسائل بمؤقت
- **مشاركة الوسائط**: صور، فيديو، وملفات آمنة
- **المجموعات والقنوات**: تواصل جماعي منظم
- **الحالات (Stories)**: مشاركة لحظات محدودة الوقت
- **البث المباشر**: تواصل فوري مع المتابعين

### 🎮 الترفيه

- **مكتبة الألعاب**: ألعاب متعددة اللاعبين
- **لوحات الصدارة**: منافسة مع الأصدقاء
- **الجلسات والدعوات**: العب مع الأصدقاء
- **Anti-Cheat**: نظام منع الغش

### 💰 المحفظة الرقمية

- **إدارة الأموال**: إرسال واستقبال الأموال
- **حلقات الادخار**: ادخار جماعي
- **الهدايا الرقمية**: إرسال هدايا للأصدقاء
- **الاستثمار**: فرص استثمار في المشاريع الناشئة

### ♿ إمكانية الوصول

- **دعم قارئ الشاشة**: TalkBack وVoiceOver
- **تكبير النصوص**: خطوط قابلة للتخصيص
- **التباين العالي**: وضع التباين المحسّن
- **تقليل الحركة**: للمستخدمين الحساسين للحركة
- **دعم التحكم بالمفاتيح**: التنقل بالكيبورد الكامل

---

## 💻 متطلبات النظام

### الحد الأدنى

| المكون | المتطلبات |
|--------|-----------|
| **نظام التشغيل** | Windows 10/11، macOS 10.15+، Ubuntu 20.04+ |
| **المعالج** | Intel Core i3 أو ما يعادله |
| **الذاكرة العشوائية (RAM)** | 8 GB |
| **مساحة التخزين** | 10 GB مساحة فارغة |
| **الإنترنت** | اتصال ثابت للتطوير |

### الموصى به

| المكون | المتطلبات |
|--------|-----------|
| **نظام التشغيل** | Windows 11، macOS 13+، Ubuntu 22.04+ |
| **المعالج** | Intel Core i7 أو Apple Silicon M1+ |
| **الذاكرة العشوائية (RAM)** | 16 GB أو أكثر |
| **مساحة التخزين** | 20 GB+ SSD |
| **الإنترنت** | اتصال عالي السرعة |

### البرمجيات المطلوبة

- **Node.js**: v20.x أو أحدث (يُفضل v22.x)
- **Bun**: v1.2.20 أو أحدث
- **Git**: v2.30+ لإدارة الكود المصدري
- **Android Studio**: لتطوير Android (اختياري)
- **Xcode**: لتطوير iOS على macOS (اختياري)

---

## 🚀 البدء السريع

### للمطورين المحترفين

إذا كنت تعرف ما تفعل، هذا كل ما تحتاجه:

```bash
# استنساخ المشروع
git clone https://github.com/your-org/mada-app.git
cd mada-app

# تثبيت Bun (إذا لم يكن مثبتاً)
npm install -g bun

# تثبيت الاعتماديات
bun install

# تشغيل التطبيق
bun expo start --web --tunnel
```

---

## 📦 التثبيت التفصيلي

### Windows + PowerShell

#### 1. تثبيت الأدوات الأساسية

##### أ. تثبيت Node.js

1. قم بتحميل Node.js من [الموقع الرسمي](https://nodejs.org/)
2. اختر نسخة LTS (v20.x أو أحدث)
3. قم بتشغيل المُثبّت واتبع التعليمات
4. تحقق من التثبيت:

```powershell
node --version
npm --version
```

##### ب. تثبيت Git

1. قم بتحميل Git من [git-scm.com](https://git-scm.com/download/win)
2. قم بتشغيل المُثبّت (اختر الإعدادات الافتراضية)
3. تحقق من التثبيت:

```powershell
git --version
```

##### ج. تثبيت Bun

افتح PowerShell كمسؤول وقم بتشغيل:

```powershell
# تثبيت Bun
irm bun.sh/install.ps1 | iex

# تحقق من التثبيت
bun --version
```

#### 2. استنساخ المشروع

```powershell
# إنشاء مجلد للمشاريع
cd $HOME
mkdir Projects
cd Projects

# استنساخ المشروع
git clone https://github.com/your-org/mada-app.git
cd mada-app
```

#### 3. تثبيت الاعتماديات

```powershell
# تثبيت جميع الحزم المطلوبة
bun install

# إذا واجهت مشاكل، جرّب:
bun install --force
```

#### 4. إعداد متغيرات البيئة

قم بإنشاء ملف `.env` في جذر المشروع:

```powershell
# إنشاء ملف .env
New-Item -Path ".env" -ItemType File

# فتح الملف بالمفكرة
notepad .env
```

أضف المحتوى التالي:

```env
EXPO_PUBLIC_RORK_API_BASE_URL=https://api.yourdomain.com
EXPO_PUBLIC_TOOLKIT_URL=https://toolkit.yourdomain.com
```

#### 5. تشغيل التطبيق

```powershell
# تشغيل على الويب
bun expo start --web

# تشغيل مع التونيل (للهواتف المحمولة)
bun expo start --tunnel

# تشغيل على Android
bun expo start --android

# تشغيل على iOS (macOS فقط)
bun expo start --ios
```

---

### Windows + Git Bash

#### 1. تثبيت Git Bash

1. حمّل Git من [git-scm.com](https://git-scm.com/download/win)
2. أثناء التثبيت، تأكد من تحديد "Git Bash Here"
3. افتح Git Bash (انقر بزر الماوس الأيمن في أي مجلد > Git Bash Here)

#### 2. تثبيت Node.js وBun

```bash
# تحقق من تثبيت Node.js
node --version

# إذا لم يكن مثبتاً، حمّله من nodejs.org

# تثبيت Bun عبر npm
npm install -g bun

# تحقق من التثبيت
bun --version
```

#### 3. إعداد المشروع

```bash
# الانتقال إلى المجلد الرئيسي
cd ~

# إنشاء مجلد المشاريع
mkdir -p Projects
cd Projects

# استنساخ المشروع
git clone https://github.com/your-org/mada-app.git
cd mada-app

# تثبيت الاعتماديات
bun install

# إنشاء ملف البيئة
cat > .env << EOF
EXPO_PUBLIC_RORK_API_BASE_URL=https://api.yourdomain.com
EXPO_PUBLIC_TOOLKIT_URL=https://toolkit.yourdomain.com
EOF

# تشغيل التطبيق
bun expo start --web --tunnel
```

---

### Linux/macOS

#### 1. تثبيت الأدوات الأساسية

##### لنظام macOS:

```bash
# تثبيت Homebrew (إذا لم يكن مثبتاً)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# تثبيت Node.js
brew install node@20

# تثبيت Git
brew install git

# تثبيت Bun
curl -fsSL https://bun.sh/install | bash

# إعادة تحميل الـ shell
source ~/.bashrc  # أو ~/.zshrc
```

##### لنظام Ubuntu/Debian:

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# تثبيت Git
sudo apt install -y git

# تثبيت Bun
curl -fsSL https://bun.sh/install | bash

# إضافة Bun إلى PATH
echo 'export PATH="$HOME/.bun/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

#### 2. إعداد المشروع

```bash
# الانتقال إلى مجلد المشاريع
cd ~/Projects || mkdir -p ~/Projects && cd ~/Projects

# استنساخ المشروع
git clone https://github.com/your-org/mada-app.git
cd mada-app

# تثبيت الاعتماديات
bun install

# إنشاء ملف البيئة
cat > .env << 'EOF'
EXPO_PUBLIC_RORK_API_BASE_URL=https://api.yourdomain.com
EXPO_PUBLIC_TOOLKIT_URL=https://toolkit.yourdomain.com
EOF

# إعطاء صلاحيات التنفيذ للسكريبتات
chmod +x scripts/*.sh

# تشغيل التطبيق
bun expo start --web --tunnel
```

---

### باستخدام Anaconda (للبيئات المعزولة)

#### 1. تثبيت Anaconda

حمّل وثبّت Anaconda من [الموقع الرسمي](https://www.anaconda.com/download)

#### 2. إنشاء بيئة مخصصة

```bash
# إنشاء بيئة جديدة
conda create -n mada-dev python=3.11 nodejs=20 -y

# تفعيل البيئة
conda activate mada-dev

# تثبيت Bun داخل البيئة
npm install -g bun

# استنساخ المشروع
cd ~/Projects
git clone https://github.com/your-org/mada-app.git
cd mada-app

# تثبيت الاعتماديات
bun install

# تشغيل التطبيق
bun expo start --web --tunnel
```

---

## 🛠️ التطوير

### هيكل المشروع

```
mada-app/
├── app/                    # صفحات التطبيق (Expo Router)
│   ├── (tabs)/            # صفحات التبويبات الرئيسية
│   ├── auth/              # صفحات المصادقة
│   ├── chat/              # صفحات المحادثات
│   ├── security/          # صفحات الأمان
│   └── _layout.tsx        # التخطيط الرئيسي
├── components/            # مكونات React القابلة لإعادة الاستخدام
├── services/              # خدمات الأعمال
│   ├── ai/               # خدمات الذكاء الاصطناعي
│   ├── security/         # خدمات الأمان
│   └── chat/             # خدمات المحادثات
├── store/                # إدارة الحالة (Zustand)
├── hooks/                # React Hooks المخصصة
├── types/                # تعريفات TypeScript
├── utils/                # أدوات مساعدة
├── constants/            # الثوابت والإعدادات
├── assets/               # الصور والموارد
├── scripts/              # سكريبتات البناء والاختبار
└── docs/                 # الوثائق التقنية
```

### الأوامر المتاحة

#### التشغيل والبناء

```bash
# تشغيل على الويب
bun expo start --web

# تشغيل على Android
bun expo start --android

# تشغيل على iOS (macOS فقط)
bun expo start --ios

# تشغيل مع التونيل
bun expo start --tunnel

# تنظيف الكاش
bun expo start --clear
```

#### الأمان والاختبارات

```bash
# فحص الأمان الشامل
bun run security-check

# تدقيق الثغرات الأمنية
bun run security-audit

# فحص ESLint للأمان
bun run lint:security

# تحليل SAST كامل
bun run sast:full

# فحص التبعيات
bun audit

# فحص CI الأمني
bun run ci:security
```

#### أدوات التطوير

```bash
# فحص TypeScript
bunx tsc --noEmit

# تنسيق الكود
bunx prettier --write "**/*.{ts,tsx,js,jsx,json,md}"

# فحص الأكواد المكررة
bun run sast:jscpd

# فحص التراخيص
license-checker --onlyAllow 'MIT;Apache-2.0;BSD-2-Clause;BSD-3-Clause;ISC;CC0-1.0'
```

### إضافة صفحة جديدة

```bash
# إنشاء صفحة جديدة في app/
touch app/new-feature.tsx
```

```typescript
// app/new-feature.tsx
import { View, Text, StyleSheet } from 'react-native';
import { Stack } from 'expo-router';

export default function NewFeatureScreen() {
  return (
    <>
      <Stack.Screen options={{ title: 'ميزة جديدة' }} />
      <View style={styles.container}>
        <Text style={styles.title}>مرحباً بك في الميزة الجديدة</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
  },
});
```

### إنشاء مكون جديد

```bash
# إنشاء مكون جديد
touch components/MyComponent.tsx
```

```typescript
// components/MyComponent.tsx
import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

interface MyComponentProps {
  title: string;
  onPress?: () => void;
}

export function MyComponent({ title, onPress }: MyComponentProps) {
  return (
    <TouchableOpacity style={styles.container} onPress={onPress}>
      <Text style={styles.title}>{title}</Text>
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: '#f0f0f0',
    borderRadius: 8,
  },
  title: {
    fontSize: 18,
    fontWeight: '600',
  },
});
```

---

## 🏗️ البنية المعمارية

### النمط المعماري

التطبيق يتبع **Clean Architecture** مع فصل واضح للمسؤوليات:

```
┌─────────────────────────────────────┐
│         UI Layer (app/)             │ ← Expo Router Pages
├─────────────────────────────────────┤
│    Components Layer (components/)   │ ← Reusable UI Components
├─────────────────────────────────────┤
│     State Management (store/)       │ ← Zustand Stores
├─────────────────────────────────────┤
│    Business Logic (services/)       │ ← Services & Use Cases
├─────────────────────────────────────┤
│      Data Layer (utils/)            │ ← Data Access & APIs
└─────────────────────────────────────┘
```

### التقنيات الأساسية

| التقنية | الوصف | الاستخدام |
|---------|-------|----------|
| **React Native** | إطار عمل للتطبيقات المحمولة | واجهة المستخدم الأساسية |
| **Expo** | منصة تطوير React Native | أدوات البناء والنشر |
| **TypeScript** | لغة برمجة مُطورة | أمان الأنواع والجودة |
| **Expo Router** | التنقل القائم على الملفات | نظام التوجيه |
| **Zustand** | إدارة الحالة | حالة التطبيق العامة |
| **Lucide Icons** | مكتبة أيقونات | الأيقونات |
| **Expo SecureStore** | تخزين آمن | البيانات الحساسة |

### الأنماط المستخدمة

- **Repository Pattern**: للوصول إلى البيانات
- **Service Layer**: لمنطق الأعمال
- **Observer Pattern**: للأحداث والإشعارات
- **Singleton Pattern**: للخدمات العامة
- **Factory Pattern**: لإنشاء الكائنات المعقدة

---

## 🔒 الأمان والخصوصية

### طبقات الحماية

#### 1. أمان البيانات

- **تشفير E2EE**: جميع الرسائل مشفرة نهاية إلى نهاية
- **Secure Enclave**: تخزين المفاتيح في معالج آمن
- **Key Rotation**: تدوير تلقائي للمفاتيح كل شهر
- **DLP**: منع تسرب البيانات الحساسة

#### 2. أمان التطبيق

- **Root/Jailbreak Detection**: كشف الأجهزة المخترقة
- **Code Injection Prevention**: منع حقن الكود
- **Anti-Tampering**: حماية ضد التلاعب
- **Certificate Pinning**: تثبيت الشهادات

#### 3. أمان المستخدم

- **MFA**: مصادقة متعددة العوامل
- **Biometric Auth**: Face ID وبصمة الإصبع
- **Progressive Lockout**: قفل تدريجي بعد محاولات فاشلة
- **Session Management**: إدارة جلسات آمنة

#### 4. المراقبة والتحليل

- **UEBA**: تحليل سلوك المستخدمين
- **Behavior Analytics**: كشف الأنماط الشاذة
- **Threat Intelligence**: معلومات التهديدات
- **SOC Integration**: تكامل مع مركز العمليات الأمنية

### التوافق مع المعايير

- ✅ **GDPR**: اللائحة العامة لحماية البيانات
- ✅ **CCPA**: قانون خصوصية المستهلك في كاليفورنيا
- ✅ **OWASP Mobile Top 10**: أفضل ممارسات OWASP
- ✅ **PCI DSS**: معيار أمان بيانات صناعة البطاقات

---

## 🧪 الاختبارات

### تشغيل الاختبارات

```bash
# اختبارات الوحدة
bun test

# اختبارات الأمان
bun run security:full-scan

# اختبارات الأداء
bun run test:performance

# اختبارات التكامل
bun run test:integration

# اختبارات E2E
bun run test:e2e
```

### تغطية الكود

```bash
# توليد تقرير التغطية
bun test --coverage

# عرض التقرير
open coverage/index.html
```

---

## 📱 النشر

### بناء للإنتاج

#### Android

```bash
# بناء APK
eas build --platform android --profile production

# بناء AAB (للنشر على Google Play)
eas build --platform android --profile production:aab
```

#### iOS

```bash
# بناء IPA
eas build --platform ios --profile production
```

#### الويب

```bash
# بناء للويب
bunx expo export:web

# رفع إلى الخادم
# نسخ محتوى dist/ إلى خادم الويب
```

### متغيرات البيئة للإنتاج

قم بإنشاء ملف `.env.production`:

```env
EXPO_PUBLIC_RORK_API_BASE_URL=https://api.production.yourdomain.com
EXPO_PUBLIC_TOOLKIT_URL=https://toolkit.production.yourdomain.com
NODE_ENV=production
```

---

## 🔧 استكشاف الأخطاء وإصلاحها

### مشاكل شائعة وحلولها

#### 1. خطأ: "getDevServer is not a function"

**السبب**: تعارض في إصدار Metro

**الحل**:
```bash
# حذف node_modules والكاش
rm -rf node_modules
bun install --force
bun expo start --clear
```

#### 2. خطأ: "Cannot read property 'isSecure' of undefined"

**السبب**: SecurityStatus غير مُهيأ بشكل صحيح

**الحل**:
```bash
# تحقق من تهيئة SecurityManager
# راجع ملف app/_layout.tsx
```

#### 3. خطأ: "Package subpath not exported"

**السبب**: تعارض في إصدارات الحزم

**الحل**:
```bash
# تحديث جميع الحزم
bun update

# أو إعادة التثبيت
rm -rf node_modules bun.lockb
bun install
```

#### 4. التطبيق لا يفتح على الهاتف

**الحل**:
```bash
# استخدم التونيل
bun expo start --tunnel

# تأكد من أن الهاتف متصل بنفس الشبكة
# أو استخدم Expo Go app
```

#### 5. مشاكل الأداء

**الحل**:
```bash
# تفعيل Hermes Engine
# راجع app.json وتأكد من:
# "jsEngine": "hermes"

# تنظيف الكاش
bun expo start --clear --no-dev --minify
```

### سجلات التصحيح

```bash
# عرض السجلات المفصلة
DEBUG=expo* bun expo start

# سجلات Android
adb logcat

# سجلات iOS
xcrun simctl spawn booted log stream
```

---

## ❓ الأسئلة الشائعة

### س1: هل يمكنني استخدام npm بدلاً من bun?

**ج**: نعم، لكن bun أسرع بكثير. إذا أردت استخدام npm:

```bash
npm install
npm run start
```

### س2: كيف أغير اسم التطبيق؟

**ج**: قم بتعديل ملف `app.json`:

```json
{
  "expo": {
    "name": "اسم التطبيق الجديد",
    "slug": "new-app-slug"
  }
}
```

### س3: كيف أضيف لغة جديدة؟

**ج**: قم بتعديل `constants/i18n.ts` وأضف الترجمات.

### س4: هل التطبيق مفتوح المصدر؟

**ج**: التطبيق خاص حالياً. للاستفسارات حول الترخيص، تواصل معنا.

### س5: كيف أبلغ عن ثغرة أمنية؟

**ج**: يُرجى إرسال بريد إلكتروني إلى: security@yourdomain.com

---

## 🤝 المساهمة

نرحب بالمساهمات! يُرجى اتباع الخطوات التالية:

### 1. Fork المشروع

```bash
# على GitHub: اضغط Fork
# ثم استنسخ نسختك
git clone https://github.com/your-username/mada-app.git
```

### 2. إنشاء فرع جديد

```bash
git checkout -b feature/amazing-feature
```

### 3. إجراء التعديلات

```bash
# قم بإجراء التعديلات
# تأكد من اتباع معايير الكود

# اختبر التعديلات
bun test
bun run lint:security
```

### 4. رفع التعديلات (Commit)

```bash
git add .
git commit -m "إضافة ميزة رائعة"
```

### 5. دفع التعديلات (Push)

```bash
git push origin feature/amazing-feature
```

### 6. فتح Pull Request

على GitHub، افتح Pull Request من فرعك إلى الفرع الرئيسي.

### معايير المساهمة

- ✅ اتبع نمط الكود الحالي
- ✅ أضف اختبارات للميزات الجديدة
- ✅ حدّث الوثائق
- ✅ تأكد من نجاح جميع الاختبارات
- ✅ اكتب رسائل commit واضحة
- ✅ لا تضف أكواد بها ثغرات أمنية

---

## 📄 الترخيص

هذا المشروع محمي بحقوق الطبع والنشر © 2025. جميع الحقوق محفوظة.

**Private License** - الاستخدام والتوزيع مقيّد.

للاستفسارات حول الترخيص:
- البريد الإلكتروني: license@yourdomain.com
- الموقع: https://yourdomain.com/license

---

## 📞 التواصل والدعم

### الدعم الفني

- **البريد الإلكتروني**: support@yourdomain.com
- **Discord**: [انضم إلى خادمنا](https://discord.gg/your-server)
- **Twitter**: [@MadaApp](https://twitter.com/madaapp)
- **الوثائق**: https://docs.yourdomain.com

### الإبلاغ عن المشاكل

- **الأخطاء**: [GitHub Issues](https://github.com/your-org/mada-app/issues)
- **الثغرات الأمنية**: security@yourdomain.com (سري)
- **اقتراحات الميزات**: [GitHub Discussions](https://github.com/your-org/mada-app/discussions)

---

## 🙏 شكر وتقدير

شكراً لجميع المساهمين والمكتبات مفتوحة المصدر التي جعلت هذا المشروع ممكناً:

- [React Native](https://reactnative.dev/)
- [Expo](https://expo.dev/)
- [Lucide Icons](https://lucide.dev/)
- والعديد من المكتبات الأخرى المدرجة في `package.json`

---

## 🗺️ خارطة الطريق

### الإصدار 1.1 (Q2 2025)

- [ ] دعم المكالمات الصوتية والمرئية
- [ ] محرر الصور المتقدم
- [ ] ملصقات متحركة
- [ ] وضع الظلام الكامل

### الإصدار 1.2 (Q3 2025)

- [ ] المجتمعات والمنتديات
- [ ] التجارة الإلكترونية المدمجة
- [ ] الذكاء الاصطناعي للترجمة الفورية
- [ ] البث المباشر المحسّن

### الإصدار 2.0 (Q4 2025)

- [ ] Web3 والعملات الرقمية
- [ ] الواقع المعزز (AR)
- [ ] منصة المحتوى للمبدعين
- [ ] تكامل IoT

---

<div align="center">

**صُنع بـ ❤️ بواسطة فريق مدى**

[الموقع الرسمي](https://yourdomain.com) • [الوثائق](https://docs.yourdomain.com) • [المدونة](https://blog.yourdomain.com)

</div>
