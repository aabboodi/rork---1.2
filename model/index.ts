import User from './User';
import Message from './Message';
import Post from './Post';
import Comment from './Comment';

export const models = [User, Message, Post, Comment];
export { User, Message, Post, Comment };
